2026-05-24

- `m_memory.jsx`를 WebGL 기반 3D 애니메이션으로 초기 구현했다.
- 화면 중앙에 조명 반응이 있는 정육면체와 강조색 모서리를 렌더링하도록 했다.
- 마우스/포인터로 정육면체 영역을 드래그하면 X/Y축 회전이 적용되고, 놓은 뒤에는 짧은 관성 회전이 남도록 했다.
- `content_item.jsx`의 M 항목에 `contextType: 'webgl'`을 추가해 Z 항목과 같은 WebGL 컨텍스트를 사용하게 했다.

2026-05-24

- 정육면체 회전 입력을 누적 X/Y 각도 방식에서 화면 기준 회전 행렬 방식으로 변경했다.
- 현재 정육면체 자세와 무관하게 마우스가 움직인 화면 방향을 기준으로 회전 델타를 적용하도록 했다.

2026-05-24

- 수평 드래그에 대응하는 Y축 회전 방향이 반대로 느껴지는 문제를 수정했다.

2026-05-24

- 정육면체 6개 면에 `M`, `E`, `M`, `O`, `R`, `Y` 글자를 각각 배치했다.
- 글자는 WebGL 텍스트가 아니라 5x7 모노스페이스 픽셀 글리프 지오메트리로 만들어 각 사각형 면 안에 맞게 렌더링했다.

2026-05-24

- 큐브 전개도 기준 배치를 위 `M`, 가운데 `E M O R`, 아래 `Y`가 되도록 다시 매핑했다.
- 사각형 큐브 면과 모서리 렌더링을 제거하고, 각 면 위치에 글자 실루엣만 남기도록 변경했다.
- 픽셀 글리프 대신 굵은 폰트 느낌의 막대형 벡터 지오메트리로 `MEMORY` 글자를 구성했다.

2026-05-24

- `MEMORY` 글자 획 두께를 전반적으로 더 두껍게 조정했다.
- 현재 회전 상태에서 화면 뒤쪽을 향하는 면의 글자는 메시 생성 단계에서 제외해 보이지 않도록 했다.

2026-05-24

- 글자 획을 아주 두꺼운 캡슐형 스트로크로 변경해 사각형 블록을 붙인 느낌을 줄였다.
- `O`는 타원 링 메시로, `R`의 볼 부분은 타원 아크 메시로 구성해 곡선 실루엣을 추가했다.

2026-05-24

- 글자 획을 한 단계 더 두껍게 키우고, 면 밖으로 넘치지 않도록 주요 획 좌표를 안쪽으로 조정했다.

2026-05-24

- `R`의 둥근 볼을 닫힌 타원 링으로 다시 만들고, 안쪽 구멍을 넓혀 더 자연스럽게 보이도록 조정했다.

2026-05-24

- 두꺼운 캡슐 획이 면 경계와 접합부에서 튀어나와 보이지 않도록 글자 중심선을 전체적으로 안쪽으로 조정했다.
- `O/R`의 곡선 반경과 두께를 줄이고, `E/Y`의 끝점을 안쪽으로 당겨 과한 돌출감을 줄였다.

2026-05-24

- `R`의 둥근 볼을 링 메시에서 채워진 타원 메시로 바꿔 안쪽 구멍이 없도록 했다.

2026-05-24

- 첫 번째 이벤트로 클릭 토글 전개 애니메이션을 추가했다.
- 접힌 상태에서 정육면체 영역을 클릭하면 전개도 형태로 펼쳐지고, 펼쳐진 전개도 영역을 다시 클릭하면 정육면체 형태로 돌아오도록 했다.
- 전개도 배치는 위 `M`, 가운데 `E M O R`, 아래 오른쪽 `Y`가 되도록 설정했다.
- 펼쳐진 상태에서는 커진 정육면체보다 작은 전개도 크기를 사용하도록 별도 `unfoldSize`를 추가했다.

2026-05-24

- 전개도에서 위쪽 `M`이 `E` 바로 위에 오도록 펼쳐진 좌표를 조정했다.

2026-05-24

- 전개도에서 다른 `M`이 `E` 옆에 남아 `E M O R` 가운데 줄을 이루도록 좌표를 수정했다.

2026-05-24

- 전개/접힘 이벤트 조건을 단일 클릭에서 짧은 두 번 탭으로 변경했다.
- 길게 누르거나 드래그한 입력은 탭으로 인정하지 않고, 탭 카운트를 초기화하도록 했다.

2026-05-24

- 각 글자 면에 서로 다른 색상을 지정했다.
- 글자를 하나의 메시로 그리던 방식을 면별 메시 렌더링으로 나누어 개별 색상이 적용되도록 했다.

2026-05-24

- 한 면을 길게 누르면 해당 면의 글자가 화면 중앙의 큰 단일 뷰로 전환되는 롱프레스 이벤트를 추가했다.
- 롱프레스는 누르는 동안 이동 거리가 작을 때만 발동하도록 해 회전 드래그와 구분했다.
- 전체 화면 상태에서 같은 면을 다시 길게 누르면 이전 큐브/전개 상태로 돌아오도록 했다.

2026-05-24

- 롱프레스 전환을 문자 확대가 아니라 선택한 면 색상의 평면 전환으로 변경했다.
- 선택된 면이 먼저 화면 정면을 향하도록 회전한 뒤, 면 중앙에서 색상 원이 퍼져 전체 화면을 덮도록 했다.
- 전체 화면 색상 평면에서 돌아오는 조건을 롱프레스가 아니라 짧은 두 번 탭으로 변경했다.

2026-05-24

- 롱프레스 전환 속도를 늦추고, 선택 면이 정면으로 회전하는 시간이 더 길게 느껴지도록 조정했다.

2026-05-24

- 롱프레스 전환 직전에 선택 면이 아닌 글자들이 사라지던 렌더 조건을 제거했다.
- 전체 화면 색상 평면에서 돌아올 때도 다른 면들이 원형 평면 뒤에 계속 렌더링되도록 해 갑자기 생기는 느낌을 줄였다.

2026-05-24

- 전개도 상태에서 롱프레스를 시작하면 전개도 배치를 유지한 채 색상 원형 전환만 일어나도록 변경했다.
- 전체 화면 색상 평면에서 돌아오는 복귀 속도를 더 느리게 조정했다.

2026-05-24

- 전체 포커스 복귀 속도는 원래대로 되돌리고, 원형 색상 평면의 확산/축소 속도만 별도 진행률로 느리게 조정했다.

2026-05-24

- 접힌 큐브 상태에서는 선택 면이 정면으로 포커스된 뒤에만 원형 색상 평면이 퍼지도록 수정했다.
- 원형 색상 평면의 확산과 축소가 같은 보간 속도를 사용하도록 정리했다.

2026-05-24

- 전체 화면 색상 평면에서 돌아올 때 원형 평면이 먼저 줄어든 뒤 포커스 회전이 원래 상태로 돌아가도록 복귀 순서를 수정했다.

2026-05-24

- 전개도 상태에서 롱프레스를 시작하면 화면 중앙이 아니라 선택한 면의 중심에서 원형 색상 평면이 퍼지도록 변경했다.
- 원형 평면은 선택된 면의 색상을 그대로 사용하고, 시작점 위치에 맞춰 화면 전체를 덮을 만큼 반경을 계산하도록 했다.

2026-06-03

- `jeju_trip.jpg`를 글자 내부 채움 이미지로 적용했다.
- WebGL 셰이더와 정점 포맷에 텍스처 좌표를 추가하고, 글자 메시만 이미지 텍스처로 렌더링하도록 변경했다.
- 롱프레스 전환의 원형 평면은 기존처럼 선택 면의 단색을 유지했다.

2026-06-03

- `jeju_trip.jpg`가 위아래로 뒤집혀 보이던 문제를 텍스처 업로드 방향 설정으로 수정했다.
- 롱프레스 전환의 원형 평면도 단색 대신 `jeju_trip.jpg` 텍스처로 채우도록 변경했다.
- 원형 전환 텍스처 좌표를 화면 기준으로 계산해 사진이 전체 화면에 자연스럽게 매핑되도록 했다.

2026-06-03

- 롱프레스 전환 화면의 이미지가 위아래로 뒤집혀 보이던 문제를 원형 전환 UV 방향으로 수정했다.
- 원형 전환 이미지가 화면 중앙에서 새로 나타나는 대신 선택한 글자 면의 이미지 중심에서 직접 확장되는 방식으로 UV 매핑을 변경했다.
- 원이 커질수록 이미지 기준 평면도 함께 커지도록 해 글자 속 사진이 전체 화면 사진으로 이어지게 했다.

2026-06-03

- 원형 전환 이미지의 최종 상태가 화면 중앙 기준 `cover` 배치가 되도록 UV 보간을 수정했다.
- 텍스처 로딩 시 이미지 가로세로 비율을 저장하고, 전환 화면에서 비율을 유지한 채 화면을 꽉 채우도록 했다.
- 선택한 글자 내부 이미지 좌표에서 시작해 최종 중앙 cover 좌표로 이어지도록 원형 전환 UV를 보간했다.

2026-06-03

- `cover` 전환 UV 수정 후 사진이 다시 위아래로 뒤집히던 문제를 글자/전환 UV의 세로 방향을 통일해 수정했다.

2026-06-03

- 글자 내부 이미지가 다시 뒤집혀 보이던 문제를 글자 메시 UV만 별도로 되돌려 수정했다.

2026-06-03

- 글자별로 다른 이미지를 지정할 수 있도록 `FACE_LETTERS`에 `image` 필드를 추가했다.
- 렌더러가 face별 텍스처를 준비하고, 같은 이미지 소스는 캐시로 한 번만 로드하도록 구조를 변경했다.
- 글자 렌더링과 롱프레스 전환 화면이 선택된 face의 텍스처와 이미지 비율을 사용하도록 수정했다.

2026-06-03

- 이미지 텍스처 렌더링에 주변 픽셀을 약하게 섞는 5탭 블러 샘플링을 추가했다.
- 블러는 텍스처를 사용하는 글자 내부 이미지와 롱프레스 전환 이미지에만 적용되도록 했다.

2026-06-03

- 글자 이미지 뒤에 같은 글자 실루엣을 조금 더 두껍게 흰색으로 먼저 렌더링해 테두리를 추가했다.
- 사용자 변경 배경색은 유지하고, 흰색 테두리 색상만 `THEME.letterOutline`으로 추가했다.

2026-06-03

- 흰 테두리와 이미지 글자가 같은 평면에서 겹쳐 번쩍거리던 z-fighting 문제를 줄이기 위해 테두리 메시를 면 안쪽 방향으로 살짝 이동했다.

2026-06-03

- M 애니메이션 배경색을 약간 매트한 하늘색 톤으로 변경했다.
